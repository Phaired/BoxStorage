# BoxStorage

## Installation


#### Install docker desktop
* ```docker compose up```

#### Connect to the database
* Execute ```history.sql```
* Then    ```insertion.sql```

Now everything should be good